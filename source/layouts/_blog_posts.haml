:ruby
  # To use this partial, you must reference this partial like so,
  # passing locals:
  #
  # = partial :blog_posts, locals: locals

  summarize ||= blog.options.summary_length

%section.articles

  - page_articles.each do |article|
    = partial :blog_post, locals: {article: article, summarize: summarize}

  = partial :blog_pagination, locals: locals
